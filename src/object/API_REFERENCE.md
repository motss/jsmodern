# API Reference

## Object

### Constructor

#### isObject(x)

Returns `true` if `x` is an `object`.

```ts
Object.isObject({}) === true;
Object.isObject({ haha: 1 }) === true;
```

### Prototype

None
